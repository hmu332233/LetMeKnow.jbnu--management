const menus = require('./menus');
const auth = require('./auth');
const userWords = require('./userWords');
const bot = require('./bot');
const addon = require('./addon');

module.exports = { auth, menus, userWords, bot, addon };
